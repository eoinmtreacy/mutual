name: Chat redeploy

on:
  push:
    branches: [ "master" ]
    paths: ["Chat/**"]

jobs:
  redeploy:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Navigating to ./Chat
      run: cd /home/ubuntu/Mutual/Chat
    - name: Rebuilding
      run: bash run.sh
