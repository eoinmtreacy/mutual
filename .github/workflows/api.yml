name: Api redeploy

on:
  push:
    branches: [ "master" ]
    paths: ["Api/**"]

jobs:
  redeploy:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Navigating to ./Api
      run: cd /home/ubuntu/Mutual/Api
    - name: Rebuilding
      run: bash run.sh
